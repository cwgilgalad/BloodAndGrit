# --- build output (regenerated by dotnet build/publish) ---
KT/source/bin/
KT/source/obj/
KT/source/publish/
KT/smoke/bin/
KT/smoke/obj/

# --- packaged deliverables (regenerated from KT/source; app/ is ~160 MB of
#     self-contained .NET runtime, the zip ~70 MB — both rebuilt on release) ---
BloodAndGrit-Keepers-Table/app/
BloodAndGrit-Keepers-Table.zip

# --- per-table runtime state (the Keeper's live session, not project source) ---
session.json
startup-error.txt

# --- python / editor debris ---
__pycache__/
*.pyc
_map_preview.html
